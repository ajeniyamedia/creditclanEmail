import { PercentageFundedPipe } from './percentage-funded.pipe';

describe('PercentageFundedPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentageFundedPipe();
    expect(pipe).toBeTruthy();
  });
});
