import { JournalapprovedPipe } from './journalapproved.pipe';

describe('JournalapprovedPipe', () => {
  it('create an instance', () => {
    const pipe = new JournalapprovedPipe();
    expect(pipe).toBeTruthy();
  });
});
