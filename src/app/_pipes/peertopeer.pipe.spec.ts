import { PeertopeerPipe } from './peertopeer.pipe';

describe('PeertopeerPipe', () => {
  it('create an instance', () => {
    const pipe = new PeertopeerPipe();
    expect(pipe).toBeTruthy();
  });
});
